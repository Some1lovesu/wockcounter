# WockCounter — Claude Code Instructions

## Git workflow
- All changes go on a feature branch (as instructed per session).
- After pushing, always create a PR targeting `main` and enable auto-merge (squash method) on it.
- Never push directly to `main`.
